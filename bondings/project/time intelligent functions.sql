select current_timestamp() as `current date & time`;
select current_date() as `current date`;
select current_time() as `current time`;
select current_user() as `user details`;
select version() as `sql version your using`;

